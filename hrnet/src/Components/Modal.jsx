/**Render of the modal when the form is validated
 * @function Modal
 * @param {JSX} children
 * @returns {JSX}
 */

export default function Modal({ children }) {
  return <div id="confirmation">{children}</div>;
}



export default function Modal({ children }) {

    // const close = () => {
    //     modal.current.style.display = "none"
    // }

    return (
        <div id="confirmation">
                {children}
            </div> 
    )
}